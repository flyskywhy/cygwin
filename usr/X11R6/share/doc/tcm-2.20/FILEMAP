Meaning of all the files in a binary distribution.

./
        CHANGELOG       (Differences between the consecutive versions).

        COPYING         (GNU public license).

        FILEMAP         (This file).

        INSTALL_        (Installation instructions)
  	    INSTALL.cygwin  (Installation instructions for Windows/CYGWIN)

        README          (README file with general info).
        README.cygwin   (README file with general info for Windows/CYGWIN).

TCM_BIN (e.g. bin/)
        tcm     (TCM startup program).
 
        tatd    (Tool for UML Activity Diagrams).
        tcbd    (Tool for UML Collaboration Diagrams).
        tcpd    (Tool for UML Component Diagrams).
        tcrd    (Tool for Class Relationship Diagrams (obsolete)).
        tdfd    (Tool for Data Flow Diagrams (subset of TEFD)).
        tdpd    (Tool for UML Deployment Diagrams).
        tefd    (Tool for Data and Event Flow Diagrams).
        terd    (Tool for Entity Relationship Diagrams (obsolete)).
        tesd    (Tool for Entity Relationship Diagrams (subset of TSSD)).
        tfet    (Tool for Function-Entity type Tables).
        tfrt    (Tool for Function Refinement Trees).
        tgd     (Tool for Generic Diagrams).
        tgt     (Tool for Generic Tables).
        tgtt    (Tool for Generic Textual Trees).
        tpsd    (Tool for Process Structure Diagrams).
        trpg    (Tool for Recursive Process Graphs).
        tscd    (Tool for UML Statechart Diagrams).
        tsnd    (Tool for System Network Diagrams).
        tssd    (Tool for UML Static Structure Diagrams).
        tstd    (Tool for State Transition Diagrams).
        ttdt    (Tool for Transaction Decomposition Tables).
        ttut    (Tool for Transaction Use Tables).
        tucd    (Tool for UML Use-Case Diagrams).
 
        psf     (PostScript filter script in Perl).
        text2ps (program that converts ASCII to PostScript).

        - statically linked distributions have also the following:
        tcmd    (all-in-one diagram editor executable)
        tcmt    (all-in-one table editor executable)

TCM_DOC (e.g. doc/)
        usersguide-<version>.pdf.gz   (TCM user's guide in gzipped PDF format)
        usersguide-<version>.ps.gz   (TCM user's guide in gzipped PostScript)
        usersguide.html   (TCM user's guide in HTML (starting point))
        usersguide/       (Directory with HTML pages of the user's guide).
        wishlist/FutureDevelopments.html (our future plans for the TCM project).
        wishlist/WishList.html (detailed list of desired additions and updates).

TCM_CONFIG (e.g. lib/)
        TCM           (X Resources. load these with xrdb or put in .Xdefaults).
 
        banner.ps     (PostScript banner page).
 
        colorrgb.txt  (Color names and their Red-Green-Blue values).

        tcm.conf      (TCM configuration file).

TCM_HELP (e.g. lib/)
        help/         (directory with text files for on-line help).

TCM_LIB (e.g. lib/)
   - dynamically linked distributions also have:
        libglobal.so  (general purpose library).
        libeditor.so  (editor library).
        libgui.so     (user interface library).
        libdiagram.so (diagram editor library).
        libtable.so   (table editor library).

TCM_MAN (e.g. man/)
        man1/         (directory with Unix man pages for each tool).
        windex        (whatis database for TCM man pages).


****************************************************************************
* More Information
****************************************************************************

For more information about TCM visit the TCM home page 
http://www.cs.utwente.nl/~tcm.

There is a TCM mailing list. Send a message to 
tcm-users-request@cs.utwente.nl for more information. For questions and 
comments you can also send an e-mail to tcm@cs.utwente.nl.

