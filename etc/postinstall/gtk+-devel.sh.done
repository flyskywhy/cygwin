#!/bin/sh
prefix=/usr
infodir=${prefix}/share/info
cd ${infodir}
for fn in gdk gtk ; do
  install-info --dir-file=./dir --info-file=${fn}.info.gz
done
for n in 1 2 3 4 5 6 ; do
  install-info --dir-file=./dir --info-file=gtk.info-${n}.gz
done

